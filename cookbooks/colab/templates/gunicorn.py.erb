import multiprocessing

bind = "127.0.0.1:8001"
workers = <%= node['config']['colab_http_workers'] || 'multiprocessing.cpu_count() * 2 + 1' %>
syslog = True
proc_name = "colab"
timeout = 90
max_requests = 1000
