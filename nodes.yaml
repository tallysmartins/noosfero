integration:
  run_list:
    - role[server]
    - role[integration_server]
email:
  run_list:
    - role[server]
    - role[email_server]
social:
  run_list:
    - role[server]
    - role[social_server]
database:
  run_list:
    - role[server]
    - role[database_server]
mezuro:
  run_list:
    - role[server]
    - role[mezuro_server]
monitor:
  run_list:
    - recipe[basics]
    - recipe[firewall]
    - role[monitoring_server]
reverseproxy:
  run_list:
    - role[server]
    - role[reverse_proxy_server]
