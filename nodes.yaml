vagrant@integration:
  run_list:
    - role[server]
    - role[integration_server]
vagrant@email:
  run_list:
    - role[server]
    - role[email_server]
vagrant@social:
  run_list:
    - role[server]
    - role[social_server]
vagrant@database:
  run_list:
    - role[server]
    - role[database_server]
vagrant@reverseproxy:
  run_list:
    - role[server]
    - role[reverse_proxy_server]
